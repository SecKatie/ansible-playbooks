---
# This is an example secret file for Grafana admin credentials
#
# To use this:
# 1. Copy this file to /tmp/grafana-secrets.yaml
# 2. Replace 'admin' and 'changeme' with your desired credentials
# 3. Seal the secret:
#    kubeseal --format=yaml < /tmp/grafana-secrets.yaml > roles/install_grafana/files/sealedsecrets.yaml
# 4. Delete the temporary file:
#    rm /tmp/grafana-secrets.yaml
# 5. Deploy with Ansible

apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin-credentials
  namespace: monitoring
type: Opaque
stringData:
  admin-user: admin
  admin-password: changeme
