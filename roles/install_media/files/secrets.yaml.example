---
# Example raw secret for ProtonVPN credentials
# DO NOT commit this file with real credentials to Git!
# Use this template to create your actual secret, then seal it with kubeseal
#
# Steps to create sealed secret:
# 1. Copy this file to /tmp/downloads-vpn-secrets.yaml
# 2. Replace the placeholder values with your actual ProtonVPN credentials
# 3. Seal the secret:
#    kubeseal --format=yaml < /tmp/downloads-vpn-secrets.yaml > roles/install_media/files/sealedsecrets.yaml
# 4. Delete the temporary file:
#    rm /tmp/downloads-vpn-secrets.yaml
#
# To get your ProtonVPN WireGuard credentials:
# 1. Log in to your ProtonVPN account at https://account.protonvpn.com/
# 2. Go to Downloads > WireGuard configuration
# 3. Select your desired server and options (port forwarding, NetShield, etc.)
# 4. Download the configuration file
# 5. Extract the PrivateKey from the [Interface] section
#
apiVersion: v1
kind: Secret
metadata:
  name: downloads-vpn-secrets
  namespace: sonarr
type: Opaque
stringData:
  # Your Mullvad WireGuard private key
  WIREGUARD_PRIVATE_KEY: "YOUR_WIREGUARD_PRIVATE_KEY_HERE"

  # Your WireGuard IP addresses (usually one IPv4 and one IPv6)
  # Format: "10.x.x.x/32,fc00:bbbb:bbbb:bb01::x:xxxx/128"
  WIREGUARD_ADDRESSES: "10.x.x.x/32"

  # Optional: Specific Mullvad server cities (comma-separated)
  # Examples: "Atlanta", "New York", "Los Angeles"
  # Leave empty to use any available server
  SERVER_CITIES: ""
