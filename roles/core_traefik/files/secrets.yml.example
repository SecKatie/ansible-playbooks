# Example secret for Cloudflare API token
# DO NOT commit actual credentials to git!
#
# 1. Create a Cloudflare API token with Zone:DNS:Edit permissions
#    - Go to: https://dash.cloudflare.com/profile/api-tokens
#    - Create Token -> Edit zone DNS template
#    - Zone Resources: Include -> Specific Zone -> mulliken.net
#
# 2. Create the raw secret:
#    cat > /tmp/cloudflare-secret.yaml <<EOF
#    apiVersion: v1
#    kind: Secret
#    metadata:
#      name: cloudflare-api-token
#      namespace: kube-system
#    type: Opaque
#    stringData:
#      api-token: "your-cloudflare-api-token-here"
#    EOF
#
# 3. Seal it:
#    kubeseal --format=yaml < /tmp/cloudflare-secret.yaml > roles/configure_traefik_acme/files/sealedsecrets.yaml
#
# 4. Clean up:
#    rm /tmp/cloudflare-secret.yaml
