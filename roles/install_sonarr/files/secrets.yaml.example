---
# Example raw secret for Mullvad VPN credentials
# DO NOT commit this file with real credentials to Git!
# Use this template to create your actual secret, then seal it with kubeseal
#
# Steps to create sealed secret:
# 1. Copy this file to /tmp/sonarr-vpn-secrets.yaml
# 2. Replace the placeholder values with your actual Mullvad credentials
# 3. Seal the secret:
#    kubeseal --format=yaml < /tmp/sonarr-vpn-secrets.yaml > roles/install_sonarr/files/sealedsecrets.yaml
# 4. Delete the temporary file:
#    rm /tmp/sonarr-vpn-secrets.yaml
#
# To get your Mullvad WireGuard credentials:
# 1. Log in to your Mullvad account at https://mullvad.net/
# 2. Go to WireGuard configuration
# 3. Generate a new WireGuard key
# 4. Download the configuration file or copy the private key and addresses
#
apiVersion: v1
kind: Secret
metadata:
  name: sonarr-vpn-secrets
  namespace: sonarr
type: Opaque
stringData:
  # Your Mullvad WireGuard private key
  WIREGUARD_PRIVATE_KEY: "YOUR_WIREGUARD_PRIVATE_KEY_HERE"

  # Your WireGuard IP addresses (usually one IPv4 and one IPv6)
  # Format: "10.x.x.x/32,fc00:bbbb:bbbb:bb01::x:xxxx/128"
  WIREGUARD_ADDRESSES: "10.x.x.x/32"

  # Optional: Specific Mullvad server cities (comma-separated)
  # Examples: "New York NY", "Los Angeles CA", "Chicago IL"
  # Leave empty to use any available server
  SERVER_CITIES: ""
