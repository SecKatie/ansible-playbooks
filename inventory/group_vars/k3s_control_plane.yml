---
# =============================================================================
# K3s Control Plane Group Variables
# =============================================================================
# These variables apply to all K3s control plane (master) nodes.
# Currently these are Raspberry Pi devices running the K3s server.
# =============================================================================

# Python interpreter (uncomment if needed)
# ansible_python_interpreter: /usr/bin/python3

# Common settings for control plane nodes
# ansible_become: yes
# ansible_become_method: sudo
