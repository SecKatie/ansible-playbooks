---
# =============================================================================
# RHEL K3s Agent Group Variables
# =============================================================================
# These variables apply to all RHEL K3s worker nodes (agents).
# =============================================================================

# SSH user for RHEL K3s agent nodes
ansible_user: kmulliken

# Python interpreter (uncomment if needed)
# ansible_python_interpreter: /usr/bin/python3

# Common settings for RHEL agent nodes
# ansible_become: yes
# ansible_become_method: sudo
