---
# =============================================================================
# Raspberry Pi K3s Agent Group Variables
# =============================================================================
# These variables apply to all Raspberry Pi K3s worker nodes (agents).
# =============================================================================

# Python interpreter (uncomment if needed)
# ansible_python_interpreter: /usr/bin/python3

# Common settings for Pi agent nodes
# ansible_become: yes
# ansible_become_method: sudo
