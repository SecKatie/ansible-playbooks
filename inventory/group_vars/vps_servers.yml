---
# =============================================================================
# VPS Servers Group Variables
# =============================================================================
# These variables apply to all VPS/cloud server instances.
# =============================================================================

# SSH user for VPS servers
ansible_user: root

# Python interpreter (uncomment if needed)
# ansible_python_interpreter: /usr/bin/python3

# Common settings for VPS servers
# ansible_become: yes
# ansible_become_method: sudo
